/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        reactStrictMode: true,
        swcMinify: true,
        appDir: true,
    },
};

module.exports = nextConfig;
